// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
/**
 * setupTests.js
 * -------------
 * Test setup file automatically loaded by Create React App (CRA).
 *
 * Imports:
 * - `@testing-library/jest-dom`: adds custom matchers to Jest (e.g., `.toHaveTextContent`)
 *
 * Purpose:
 * Enhances unit testing capabilities by extending default matchers.
 */
import '@testing-library/jest-dom';
